    var number = 3;
        
    if (number > 0 && number < 4) {

        if (number == 1) {
            console.log("We're inside the first branch and the first nested condition.");
        }

        if (number == 2) {
            console.log("We're inside the first branch and the second nested condition.");
        }

        if (number == 3) {
            console.log("We're inside the first branch and the third nested condition.");
        }
    }
    
    else if (number > 3 && number < 7) {

        if (number == 4) {
            console.log("We're inside the second branch and the first nested condition.");
        }

        if (number == 5) {
            console.log("We're inside the second branch and the second nested condition.");
        }

        if (number == 6) {
            console.log("We're inside the second branch and the third nested condition.");
        }
    }

